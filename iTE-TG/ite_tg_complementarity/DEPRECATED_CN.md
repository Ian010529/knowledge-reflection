# 作废说明

状态：**不可用于发现、排序、验证或正对照统计**。

本目录的方案由预写 `PROGRAM_RULES`、人工 mechanism cards 与 curated
compatibility rules 生成；随后又被投影回 pair graph，形成循环论证。审计还
确认部分 concept（例如 P0337 的 `redox entropy`）没有原摘要证据。

保留原因仅为复盘错误来源。替代结果：`../cleanroom_abstract_pair_layer/`。
