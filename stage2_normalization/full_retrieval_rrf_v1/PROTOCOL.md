# 新修订数据的关键词＋向量 RRF 试跑

仅处理 full_audit_v1/final/revised_all.json 中 accepted 关系的主体/客体局部节点。accepted 是已有流程状态，不是本次语义验收。131条待定关系不参与生成检索输入，另存ID；其端点若也出现在接受关系中仍可参与。同篇同ID复用，不提前跨论文合并，也不新增独立联合因素节点。

沿用旧试验参数：名称BM25（k1=1.2,b=0.75）与名称＋原文短上下文的Nomic v1.5向量各Top-10；等权RRF，常数60，最终Top-10。同域检索，排除自身，允许同篇；角色只作展示/诊断，不参与过滤。BM25只保留正分。排序并列按局部节点ID稳定打破。未换模型、调参或做跨域匹配。

输入适配：同节点可关联多个证据片段，优先选择含节点名称的原文片段，并调用既有短句提取函数；名称是概括而未逐字出现时，选择首条关联原文证据的前80词，记录fallback。该上下文仅供检索，不替代节点保留的完整证据列表。新旧数据构成不同，不能直接比较两次样本分数或沿用旧等价标签。

冻结种子20260925，每域12个节点用于展示Top-10；在结果返回前从中固定每域6个用于三方法Top-1诊断。诊断为本会话探索性模型判断，看到候选与原文、非盲评、非独立金标准；不作全库精确率、召回率或融合优越性估计。

只产出向量缓存、排名、候选对和对照报告。不调用生成式模型逐对任务，不合并节点，不改变抽取或历史结果。代码复用旧BM25分词/参数及上下文函数；本地使用主检出目录中可用的embedding环境，当前worktree的PyTorch大文件为LFS占位符，无须修改旧环境。
