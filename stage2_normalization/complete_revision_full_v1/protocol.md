# 全库完整摘要一次修订与预留验收

2026-09-25用户在了解开发结果及分母后明确指令：“那全库推进一下”。这是一项新的扩量授权，保存expansion_decision.json，不改写complete_revision_v1原始开发评分、冻结协议或停止记录。

沿用开发阶段的同一revision_prompt和schema、Astra-medium、每批初始4篇、最多4路并发。长输入自动减批，摘要和所有旧关系均完整保留。1971篇中复用开发40篇，只对另外1931篇各修订一次。程序保存所有旧ID的唯一处置、patch实际变化、新增ID、拒绝、待定和按篇重抽标记；每批保存检查点，已成功任务不重新调用。技术失败至多一次重试；认证/权限/额度失败停止派发，不切换身份、API、模型或自动重置额度。

只有模型标记为样品/指代/主体客体结构混乱、无法局部恢复的论文，另用只含完整原文和冻结规则的新上下文重抽一次；再与旧版比对正确关系保留情况。原文损坏不靠重抽猜测。没有第三、第四轮语义修订。

完整摘要参考只为验收样本建立，不为全库每篇重复建立参考或盲评。验收样本排除本次40篇和其非空DOI重复项；其余历史接触情况逐项记录，不冒充首次接触或人工金标准。提前按年份层随机保留每域30篇，空关系论文也可入样；源参考只看原文，先保存并冻结，再与最终结果比较。参考的valid/uncertain/out_of_scope判断及争议全部保留，不按成绩修改参考。

修订及必要重抽完成后，在同一排除框内按年份×核心/外围scope×证据复杂度分层抽每域100条接受关系。主指标为接受记录严格精确率（supported/全部样本，N/n设计权重）；partial/unsupported/out_of_scope/uncertain均不算严格正确。另报告待定规模，不能把生产接受标签当成正确性结论。覆盖以冻结参考原子关系计，报告有效参考完整覆盖及包括待定参考的保守完整覆盖、核心和全范围各自分子分母。所有区间沿用按论文聚类/分层重采样bootstrap 2000次的95%近似区间，并说明不包含模型系统偏差。目标为两域分别约90%严格精确率、约70%核心覆盖，点估计过线不证明总体稳定达标。

拒绝原关系<=200时全部独立核查；更多时按域每域最多100条、按年份分层N/n权重抽查，保留各样本抽样概率。拒绝判断单独报告，不能以只保留容易记录来达标。验收反馈不再进入本轮修订或重抽；低分照实报告，不无限重试。

沿用已完成的三个描述局部修订，不重做全库归一化；原concept ID和原成员映射复用。新增或改变含义的端点单独检查映射，证据不足保持独立/待定，不自动同名合并。验收后另存新版双域图谱，明确验收是否过线，不覆盖旧图；未过线不得称质量已验收图谱。

主要交付：新版关系总集、逐项变更记录、待定/拒绝/重抽清单、验收报告；新版图谱作为派生输出。原始数据、旧图、历史模型输出与开发参考保持冻结。只读程序检查不是语义验收。
